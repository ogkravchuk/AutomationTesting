CREATE TABLE `lesson4`.`airlines` (
  `flightNumber` INT NOT NULL,
  `airline` VARCHAR(45) NULL,
  `additionalSpaceService` VARCHAR(45) NULL,
  `webRegistration` VARCHAR(45) NULL,
  `isMealincluded` VARCHAR(45) NULL,
  `airlinescol` VARCHAR(45) NULL,
  PRIMARY KEY (`flightNumber`));